@page
@model Testare_TravelingApp.Pages.Agendas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Agenda[0].Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Agenda[0].DateCreated)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Agenda[0].User)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Agenda) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateCreated)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.AgendaId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.AgendaId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.AgendaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
